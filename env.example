# ============================================
# TeleCode Configuration
# Rename this file to .env and fill in your values
# ============================================
#
# SECURITY NOTE:
# When you use the GUI setup, your bot token is stored in an
# ENCRYPTED VAULT (not this file). This provides better security.
#
# The vault uses:
#   - Windows: DPAPI encryption (machine-bound)
#   - macOS: Keychain Services
#   - Linux: Encrypted file with machine-specific key
#
# If the vault is active, you'll see:
#   TELEGRAM_BOT_TOKEN=[STORED_IN_SECURE_VAULT]
#
# ============================================

# Your Telegram Bot Token (get from @BotFather)
# SECURITY: Use the GUI setup to store this in the encrypted vault instead!
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Your Telegram User ID (get from @userinfobot)
ALLOWED_USER_ID=123456789

# Root directory for sandboxed operations
# All file operations are restricted to this folder
DEV_ROOT=C:\Users\YourName\Projects

# Optional: Enable voice transcription (requires ffmpeg)
ENABLE_VOICE=true

# Optional: Prevent system sleep while bot is running
PREVENT_SLEEP=true

# Optional: Enable audit logging
ENABLE_AUDIT_LOG=true

# Optional: Default AI model (can be changed per-user via /model command)
# Available: opus, sonnet, haiku, gemini, gpt
# Default is 'opus' (Claude Opus 4.5) if not specified
DEFAULT_MODEL=opus

